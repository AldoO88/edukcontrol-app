// =====================================================================
// app/(director)/(tabs)/dashboard.jsx
// ---------------------------------------------------------------------
// Ruta "/dashboard" del tab "Inicio" del director.
// Renderiza el DirectorDashboard con métricas globales.
// =====================================================================

import React from 'react';
import DirectorDashboard from '../_components/DirectorDashboard';

export default function DirectorDashboardTab() {
  return <DirectorDashboard />;
}
